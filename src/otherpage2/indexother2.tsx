
import React from "react";
import ReactDOM from 'react-dom';
import AppOther from './AppOther2';

ReactDOM.render(
  <React.StrictMode>
    <AppOther />
  </React.StrictMode>,
  document.getElementById('root')
);
